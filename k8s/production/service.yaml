apiVersion: v1
kind: Service
metadata:
  name: demo-app-svc
  namespace: production

spec:
  type: NodePort

  selector:
    app: demo-app

  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30081
